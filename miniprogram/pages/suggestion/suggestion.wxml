<!-- index.wxml -->
<view class="page">
  <view class="page__bd">
    <!-- <scroll-view scroll-y="true" class="container" bindscrolltoupper="upper" upper-threshold="10" lower-threshold="5" bindscrolltolower="lower" scroll-into-view="{{toView}}" scroll-top="{{scrollTop}}"> -->
    <block wx:for="{{suggestion}}" wx:for-index="idx" wx:key="_id" wx:for-item="item" data-idx="{{idx}}">
      <view class="weui-panel weui-panel_access">
        <article-item articleType="suggestion" timeStamp="{{item.timeStamp}}" articleId="{{item._id}}" title="{{item.title}}" detail="{{item.detail}}" userId="{{item.userId}}" date="{{item.date}}"></article-item>
      </view>
    </block>
    <!-- </scroll-view> -->
  </view>
</view>
<my-fab-button></my-fab-button>