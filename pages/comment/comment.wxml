<view class="page">
    <view class="page__bd">
        <scroll-view scroll-y style="padding-bottom:70px" bindscrolltoupper="upper" bindscrolltolower="lower" bindscroll="scroll" scroll-into-view="{{toView}}" scroll-top="{{scrollTop}}">
            <view class="weui-panel">
                <view class="weui-panel weui-panel_access">
                    <view class="weui-panel__bd">
                        <block wx:for="{{comment}}" wx:for-index="idx" wx:key="_id" wx:for-item="item" data-idx="{{idx}}">
                            <view class="weui-media-box weui-media-box_text">
                                <view class="weui-media-box__title weui-media-box__title_in-text">
                                    <image src="{{item.avatar}}" />
                                    <view class="" selectable="false" space="false" decode="false">
                                        {{item.username}}
                                    </view>
                                </view>
                                <view class="weui-media-box__desc">{{item.content}}</view>
                                <view class="weui-media-box__info">
                                    <view class="weui-media-box__info__meta">
                                        {{item.username}}
                                    </view>
                                    <view class="weui-media-box__info__meta weui-media-box__info__meta_extra">
                                        {{item.date}}
                                    </view>
                                </view>
                            </view>
                        </block>
                    </view>
                </view>
            </view>
        </scroll-view>
        <view class="weui-btn-area" style="position:fixed;bottom:10px;width:100%;margin:0">
            <button style="margin-top: 30rpx;width: 700rpx;" class="weui-btn" type="default" bindtap="onTapToAddComment">
                我要评论
            </button>
        </view>
    </view>
</view>